import { WelcomepipePipe } from './welcomepipe.pipe';

describe('WelcomepipePipe', () => {
  it('create an instance', () => {
    const pipe = new WelcomepipePipe();
    expect(pipe).toBeTruthy();
  });
});
